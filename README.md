# Интернет-магазин на Django

Учебный проект интернет-магазина, разрабатываемый в рамках курса по Django.

## Описание

Проект представляет собой базовую структуру интернет-магазина с главной страницей и страницей контактов.
На каждом уроке курса функциональность проекта будет расширяться.

## Технологии

- Python 3
- Django 6
- Bootstrap 5

## Установка и запуск

1. Клонируйте репозиторий:

```bash
git clone <url-репозитория>
cd <название-проекта>
```

2. Создайте и активируйте виртуальное окружение:

```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/macOS
source venv/bin/activate
```

3. Установите зависимости:

```bash
pip install -r requirements.txt
```

4. Примените миграции:

```bash
python manage.py migrate
```

5. Запустите сервер разработки:

```bash
python manage.py runserver
```

6. Откройте в браузере:

- Главная страница: http://127.0.0.1:8000/
- Контакты: http://127.0.0.1:8000/contacts/

## Структура проекта

```
.
├── catalog/              # Приложение каталога
│   ├── templates/        # HTML-шаблоны
│   ├── urls.py           # Маршруты приложения
│   └── views.py          # Контроллеры
├── config/               # Настройки Django-проекта
├── manage.py
├── requirements.txt
└── README.md
```

## GitFlow

- `main` — стабильная ветка
- `develop` — ветка разработки
- `homework/*` — ветки для домашних заданий

Домашние задания сдаются через pull request из ветки домашней работы в `develop`.

## Автор

Учебный проект курса «Знакомство с Django».
